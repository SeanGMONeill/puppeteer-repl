Launches user's own install of Chrome in 'inspect' mode, and initializes a repl window to control the Chrome window with Puppeteer

To install:
npm install

To run:
npm run

Also requires that Chrome is installed.